# 🛰️ PrimeStay AIC 統合治理中心：子任務完成報告

## 1. 任務背景
原有的管理視窗分散於 `/admin/organizations` (組織)、`/admin/users` (用戶) 與 `/admin/management` (資產樹)，導致管理員在執行治理決策時需頻繁切換頁面，且缺乏操作前的連鎖影響評估。

## 2. 核心變更說明

### 2.1 介面統合 (Zero-Scroll 治理終端)
- **單一入口**：以 `/admin/management` 作為唯一治理解析終端。
- **情境感知面板 (Command Vault)**：
    - 點擊 **組織**：自動加載 `OrgPlanManager` 組件。
    - 點擊 **人員 (Landlord/Manager/Tenant)**：自動加載 `UserStatusToggle` 組件。
- **雙模支援**：在桌面版使用右側常駐面板，手機版自動切換至 `QuickActionDrawer`，功能 100% 同步。

### 2.2 治理安全與決策輔助 (Impact Advisor)
- **風險告知**：根據 `docs/roles.md` 的停權影響矩陣，實作 `GovernanceImpactAdvisor` 組件。
- **預期影響提示**：在停權房東前，系統會明確提示「將隱藏旗下房源」與「凍結代管權限」，防止誤操作。
- **文件規格統合**：重構 `spec.md`，將重複的角色定義內容移除，改為直接引用 [`docs/roles.md`](docs/roles.md)，確保系統規格與權限定義來源唯一化 (SSOT)。

### 2.3 視覺索引優化 (Nexus Pulse)
- **狀態聯動**：樹狀圖節點脈動燈號與文字樣式現在能即時反应 `SUSPENDED` 狀態（灰階熄滅、刪除線）。
- **血緣追蹤**：強化頂部麵包屑的層級導航能力。

### 2.4 系統精簡與冗餘清理
- **路由重導向**：於 `next.config.mjs` 實作 301 永久重導向，將舊有的 `/admin/organizations` 與 `/admin/users` 導向至 `/admin/management`。
- **組件中心化**：將治理邏輯 (`OrgPlanManager`, `UserStatusToggle`) 提取至 `src/components/governance`，解決循環依賴問題。
- **代碼精簡**：刪除已廢棄的舊頁面資料夾，並更新 `AppSidebar` 將選單項目整合為「治理中心」。

## 3. 實作檔案清單
- `src/components/management/ManagementViewWrapper.tsx`: 實作動態 Content Switcher 與指揮面板。
- `src/components/management/ManagementTree.tsx`: 優化停權節點之視覺特徵與 URL SearchParam 聯動。
- `src/components/governance/`: 存放中心化治理邏輯組件。
- `next.config.mjs`: 配置路由重導向。
- `docs/management_integration_spec.md`: 整合設計規範。
- `spec.md`: 更新全局治理規格。

## 4. 下一步建議
- 增加批次停權 (Bulk Action) 功能於 Management Tree 的多選模式。
- 整合操作日誌 (Audit Log) 至 Command Vault 底部，顯示該實體的操作歷史。

---
*Report generated by PrimeStay Architect Agent - AIC v3 Governance.*